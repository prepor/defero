<% if params[:promt] %>
	var login = prompt("Login", "");	
<% end %>

new Juggernaut({"channels":["<%= params[:chat_id] %>"],
	"height":"0px",
	"session_id":<% if params[:check_code] %>"<%=params[:check_code]%>"<% else %>null<% end %>,
	"bridge_name":"juggernaut",
	"swf_address":"\/juggernaut.swf",
	"reconnect_attempts":3,
	"ei_swf_address":"\/juggernaut\/expressinstall.swf",
	"host":"127.0.0.1","reconnect_intervals":3,
	"width":"0px",
	"flash_version":8,
	"debug":true,
	"client_id":<% if params[:promt] %>login<% else %>"<%= params[:username] %>"<% end %>,
	"port":5001,
	"flash_color":"#fff",
	"swf_name":"juggernaut_flash"});
		
